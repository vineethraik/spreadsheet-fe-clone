import type { JSX } from 'react';
import './TopBar.scss';

function TopBar(): JSX.Element {
    return <div className="top_bar flex flex-row items-center justify-between">top bar</div>;
}

export default TopBar;